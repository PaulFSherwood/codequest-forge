# Dungeon Walk Reference

`dungeon_walk_final.adb` is the fully commented program supplied by the learner.

Use it only after attempting the current milestone. The curriculum intentionally
builds toward this program in small steps instead of asking the learner to paste
the complete source at the beginning.

The final milestone then refactors the working monolithic program into packages
without changing its behavior.
